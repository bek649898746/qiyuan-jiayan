# 启元·甲言 (qiyuan-jiayan) 代码审计报告

**审计日期**: 2026-08-09  
**审计范围**: 编译器(jycc.c)、链接器(jyld.c)、后端(qcc_x86.c)、运行时(qcc_rt.c)、测试用例  
**审计类型**: 全量代码审查 - BUG定位 + 改进建议  

---

## 一、严重 BUG (P0 - 立即修复)

### BUG-1: `qcc_rt.c` `add_glo` 中 `realloc` 返回后 `stc_a` 指针悬空 (严重)

**文件**: `qcc_rt.c`  
**位置**: `add_glo` 函数 (约 L150-180 区域)  
**描述**: 在 `add_glo` 中调用 `stc_a = realloc(stc_a, ...)` 后，如果 `realloc` 移动了内存块，之前通过 `glo_init` 或别处获取的指向 `stc_a` 旧地址的指针将全部悬空。

**复现条件**: 当 `total_stc_n` 增长触发 `realloc` 移动底层内存时。  
**影响**: 静默数据损坏，访问已释放的内存（use-after-free），崩溃或生成错误代码。  

**修复建议**: 
- 使用 `malloc`+`memcpy`+`free` 代替直接 `realloc` 并显式更新所有指向旧地址的指针
- 或在架构层面使用索引而非裸指针引用结构体成员

---

### BUG-2: `qcc_x86.c` `cr_mov_ri64` 边界检查不足 (严重)

**文件**: `qcc_x86.c`  
**位置**: `cr_mov_ri64` 函数  
**描述**: 直接向 `buf[p]` 写入而不检查 `p` 是否超出缓冲区大小。若 `p+10 >= buf_size` 将导致缓冲区溢出。

**修复建议**: 在函数入口或每次写入前增加 `p + 字节数 <= buf_size` 的断言/检查。

---

### BUG-3: `jyld.c` `write_pe` 中 `data_raw` 取整可能为 0 (严重)

**文件**: `jyld.c`  
**位置**: 第 1026-1027 行  
```c
int data_raw = msvcrt_end + out_len[OUT_DATA];
data_raw = (data_raw + 511) & ~511;
if (data_raw < 0x200) data_raw = 0x200;
```
**描述**: 当 `msvcrt_end + out_len[OUT_DATA]` 值很小时（比如 < 512），`data_raw` 确实会被设回 0x200，这是正确的。但在 `data_raw >= 0x200` 时才走正常路径，`data_foff + data_raw` 决定了 `.data` 节在文件中的结束。如果 `data_raw` 计算值恰好小于 `data_foff`（比如 text 节很大），会导致后续 `fseek` 可能定位到负偏移。

**修复建议**: `data_raw` 应该基于实际需要的大小计算，且不应小于 `0x200`，但更关键的应该是确保 `data_foff + data_raw` 不超过 `text_foff + text_size` 造成覆盖。

---

### BUG-4: `jyld.c` 导入区布局偏移不一致 (严重)

**文件**: `jyld.c`  
**位置**: `write_pe` 函数中 `k32_ilt`、`msvcrt_iat_off`、`msvcrt_end` 等多处  
**描述**: 多个地方（第1078-1130行）分散计算 kernel32 和 msvcrt 的 ILT/IAT/hint 布局偏移，且部分采用硬编码偏移（如 `k32_dll_off + 13`），部分使用变量计算（如 `mdesc_off`）。如果布局逻辑不一致，可能导致 IAT/ILT 指向错误位置，PE 加载器无法正确解析导入。

**修复建议**: 
- 将所有导入区布局计算集中到一个 `layout_imports()` 函数中
- 使用结构化成员表示各个区域偏移
- 在写 PE 时统一使用这些预先计算好的偏移
- 增加自校验：写完 ILT 后回读验证

---

### BUG-5: `qcc_rt.c` 局部字符串常量段查询越界风险 (中等→严重)

**文件**: `qcc_rt.c`  
**位置**: `tok_dat` 函数及字符串表查找  
**描述**: 使用 `!strcmp(gstr[i], tok)` 查找字符串时，如果 `gstr` 总数超过 `gstr_n` 且缺少边界检查，可能读到已释放/无效内存。

**修复建议**: 确保所有遍历 `gstr` 的地方以 `gstr_n` 为上限；`gstr` 增长后确保 `gstr_n` 同步更新。

---

## 二、中等 BUG (P1 - 尽快修复)

### BUG-6: `qcc_rt.c` 函数调用参数求值顺序依赖未定义行为

**文件**: `qcc_rt.c`, `qcc_x86.c`  
**位置**: 多处类似 `push_arg(buf, &p, expr(left), ...)` 的调用  
**描述**: C 标准未规定函数参数的求值顺序。如果 `expr(left)` 和 `expr(right)` 有共享副作用（如修改全局状态 `addr_p`、`sp_off`），不同编译器可能产生不同的结果。gcc/clang 的求值顺序通常是右到左，但不可依赖此行为。

**修复建议**: 将参数求值拆分到临时变量中，显式控制求值顺序：
```c
int lv = expr(left);
int rv = expr(right);
push_arg(buf, &p, lv, rv);
```

---

### BUG-7: `jycc.c` 移位操作左操作数类型不当

**文件**: `jycc.c`  
**位置**: 多处 `1 << n` 表达式，n 为 int 类型  
**描述**: 当 `n >= 31` 时，`1 << n` 在 32 位 int 上是未定义行为（有符号整数溢出）。

**修复建议**: 使用 `1ULL << n` 或 `(unsigned)1 << n`。

---

### BUG-8: `qcc_rt.c` `malloc` 返回值未检查

**文件**: `qcc_rt.c`, `jycc.c`  
**位置**: 多处  
**描述**: `malloc`/`realloc`/`calloc` 调用后直接将返回值赋值给指针，未检查是否为 NULL。在内存不足时会导致空指针解引用崩溃。

**修复建议**: 增加 NULL 检查，失败时输出错误信息并 clean exit。

---

### BUG-9: `jyld.c` 链接器字符串比较使用中文符号 `"主"` 查找 main

**文件**: `jyld.c`  
**位置**: 第 1183 行  
```c
if (!strcmp(gsyms[i].name, "main") || !strcmp(gsyms[i].name, "主"))
```
**描述**: 使用 Unicode 中文 "主" 作为 main 函数的别名查找。这要求编译器将 "main" 编码为 "主" 存入符号表，增加了不必要的不一致性。且 `strcmp` 对多字节字符的逐字节比较在某些编码下可能错误。

**修复建议**: 统一使用英文 "main" 作为入口符号名，或使用 `wcscmp` 处理宽字符。

---

## 三、安全/鲁棒性建议 (P1/P2)

### BUG-10: 编译器/链接器缺乏对恶意输入的保护

**文件**: `jycc.c`, `jyld.c`  
**描述**: 
- COFF 解析时直接使用文件中的 size/offset 字段做 `memmove`/`fseek`，未充分验证范围
- 如果输入文件被篡改（如 offset 指向文件范围外），可能导致越界读
- 符号数量 `nsyms` 直接用于分配数组，恶意构造过大值将耗尽内存

**修复建议**:
- 在解析 COFF 头后验证 section/符号表的 offset+size 不超出文件范围
- 对 `nsyms` 设置合理上限（如 100000）

---

## 四、架构与工程建议

### S1: 代码整体结构问题

1. **单文件过大**: `jycc.c` ~2000 行，`qcc_rt.c` ~1600 行，`jyld.c` ~1234 行。建议拆分为多个翻译单元
2. **全局状态过多**: `jycc.c` 中有大量全局变量（`tok`, `tk`, `tki`, `last_id`, `lx`, `cur_func`, `loop_nest` 等），使得代码难以测试和复用
3. **无测试框架**: 项目缺少自动化测试。建议引入最简测试框架，对编译器管道进行端到端验证
4. **硬编码常量**: 大量 magic number 散落各处（如 `0x5000000`、`0x400000`、`0x1000`、`4095`），建议定义为有意义的宏

### S2: 内存管理问题

1. **无内存释放**: 整个项目 `malloc` 之后从不 `free`（链接器在 `main` 末尾无清理）。对于一次性工具影响较小，但不规范
2. **`realloc` 模式脆弱**: 多处使用 `X = realloc(X, new_size)` 的直接模式，若 `realloc` 失败返回 NULL，原始指针丢失造成内存泄漏

### S3: 错误处理不一致

1. 某些地方使用 `fprintf(stderr, ...)` + `return 1`，有些地方用 `exit(1)`
2. 部分错误路径未输出足够的诊断信息（如 COFF 解析失败时未输出文件名和偏移）

### S4: 缺少 `.pdata` / 异常处理目录

- `jyld.c` 写 PE 时填充了 `g_pdata_rva` / `g_pdata_len`（Exception Directory），但代码中是否有地方生成实际的 `.pdata` 内容不确定。如果没有对应的 unwind 信息，x64 PE 在发生异常时行为未定义。

### S5: 中英文混合命名

- 启元·甲言项目混合使用中文关键词和英文实现，如用 `"主"` 表示 `main`、`"甲言"` 作为语言名。这增加了国际化和可维护性的负担，且 `strcmp` 对多字节字符不可靠

---

## 五、测试用例审查

### `_g1_main.c` (sqlite3 测试)
- 功能: 测试链接 sqlite3.dll
- 问题: 仅测试 `sqlite3_open` / `exec` / `close`，未覆盖错误路径（如打开失败、SQL 语法错误时 `sqlite3_errmsg` 的调用）
- 建议: 增加一个故意失败的测试用例

### `_g1_a.c`, `_g1_b.c`, `_g1_empty.c`
- 作为多目标链接的回归测试，结构合理但覆盖率有限

### 测试覆盖缺口
- 缺少浮点运算密集型测试
- 缺少递归调用测试
- 缺少大数组/结构体测试
- 缺少长标识符测试
- 缺少错误输入的边界测试（语法错误、不完整 COFF 文件等）

---

## 六、汇总优先级

| 优先级 | 编号 | 简述 | 影响 |
|--------|------|------|------|
| P0 | BUG-1 | realloc 后指针悬空 | 静默数据损坏/crash |
| P0 | BUG-2 | 缓冲区溢出 | 安全漏洞/crash |
| P0 | BUG-4 | PE 导入区布局不一致 | 生成无效 PE |
| P0 | BUG-3 | data_raw 计算覆盖风险 | PE 布局错误 |
| P1 | BUG-5 | 字符串表越界 | crash |
| P1 | BUG-6 | 参数求值顺序 UB | 编译器行为不一致 |
| P1 | BUG-7 | 有符号整数移位溢出 UB | 未定义行为 |
| P1 | BUG-8 | malloc 未检查 NULL | crash |
| P1 | BUG-9 | strcmp 比较中文符号 | 编码兼容性问题 |
| P1 | BUG-10 | 缺少输入验证 | 安全漏洞 |
| P2 | S1-S5 | 架构/工程改进 | 可维护性/可测试性 |

---

## 七、总结

启元·甲言是一个令人印象深刻的国产自研编译器+链接器项目，从零实现了 C 子集到 x86-64 PE 的全链路。代码整体逻辑清晰，核心算法（如 COFF 解析、x86 代码生成、PE 导出写入）实现正确。

主要的 P0 级 BUG 集中在**内存安全**（realloc 悬空指针、缓冲区溢出）和 **PE 格式正确性**（导入表布局不一致）。这些问题在特定输入下会导致崩溃或生成不可用的二进制文件。建议优先修复这 4 个 P0 问题，并引入自动化测试防止回归。

**项目闪光点**:
- 自研编译器前后端全链路打通，无第三方依赖
- COFF 解析和 PE 生成逻辑完善，支持多目标链接、静态库(.a)归档文件、多 DLL 导入
- x86-64 代码生成覆盖了完整的 C 子集（结构体、浮点、函数调用 ABI）
- 运行时代码生成和元编程能力（`甲言` 宏系统）是独特的创新点